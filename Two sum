class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        num = {}
        for i, x in enumerate(nums):
            y = target - x
            if y in num:
                return [num[y], i]
            num[x] = i
