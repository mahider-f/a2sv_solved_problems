class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        a=Counter(nums)
        b=[]
        for x,i in a.items():
            if i>(len(nums)//3):
                b.append(x)
        return b

