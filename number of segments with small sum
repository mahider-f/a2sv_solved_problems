n, s = map(int, input().split())
nums = list(map(int, input().split()))

left = 0
cur_sum = 0
ans = 0

for right in range(n):
    cur_sum += nums[right]

    while cur_sum > s:
        
        cur_sum -= nums[left]
        left += 1
    ans += (right - left+1)
    
print(ans)
