class Solution:
    def missingNumber(self, nums: List[int]) -> int:
        nums=sorted(nums)
        a=0
        c=0
        for i in range (0, len(nums)):
            a+=nums[i]
        for j in range(0,len(nums)+1):
            c+=j
        b=c-a
        return b
