class Solution:
    def strStr(self, haystack: str, needle: str) -> int:
        if not needle:
            return 0
        a = len(haystack)
        b = len(needle)
        
        for i in range(a-b+1):
            if haystack[i:b+i]== needle:
                return i
        return -1
         
